// Выставляем WS_PORT=0 до запуска тестов.
// Порт 0 означает «занять любой свободный порт» — это позволяет
// запускать e2e-тесты даже если dev-сервер уже занял порт 8080.
module.exports = async () => {
  process.env.WS_PORT = '0';
  process.env.PORT = '0';
};
